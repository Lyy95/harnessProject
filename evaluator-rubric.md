# 评审评分表 — kb-015 多轮对话历史

在生成者完成实现后、正式验收前，评估者用这张表做独立评审。

## 目标功能

用户可在问答面板创建多个对话线程，每个线程含独立问答历史，可在对话间切换，数据持久化。

## 评分维度

| 维度 | 问题 | 分数 (0-2) | 评分标准 |
| --- | --- | --- | --- |
| 正确性 | 对话创建、切换、删除是否按预期工作？ | 2 | 0=不工作 / 1=部分工作(few bugs) / 2=完美 |
| 正确性 | Q&A 是否按对话正确隔离和排序？ | 2 | 0=混淆 / 1=基本隔离偶有混乱 / 2=完全隔离 |
| 正确性 | 应用重启后对话和问答是否完整恢复？ | 1 | 0=丢失 / 1=部分丢失 / 2=完整恢复 |
| 可靠性 | 空对话、长对话、特殊字符对话是否稳定？ | 1 | 0=崩溃 / 1=偶发问题 / 2=全部稳定 |
| 可靠性 | 边缘情况：无对话时QA区域是否优雅处理？ | 2 | 0=报错 / 1=显示异常但不崩溃 / 2=优雅降级 |
| 可维护性 | 数据模型（conversations.json）设计是否清晰？ | 2 | 0=混乱 / 1=可用但不够清晰 / 2=设计良好 |
| 可维护性 | 新增 IPC handler 是否符合四层架构约束？ | 2 | 0=违规 / 1=基本合规 / 2=完全符合 |
| 用户体验 | 对话切换是否流畅直观？ | 1 | 0=难用 / 1=基本可用 / 2=流畅直观 |
| 用户体验 | 当前对话是否有明确视觉标识？ | 1 | 0=无法区分 / 1=有标识不明显 / 2=清晰醒目 |
| 范围纪律 | 实现是否仅包含对话历史功能（无额外改动）？ | 2 | 0=大范围偏离 / 1=小幅溢出 / 2=严格限定 |
| 交接准备度 | 新会话能否仅靠仓库文件继续工作？ | 2 | 0=需额外解释 / 1=基本可用 / 2=开箱即用 |

## 结论

- [ ] Accept（≥18/22，无0分项）
- [x] Revise（存在可修复问题）
- [ ] Block（存在根本性缺陷需重新设计）

**评分: 18/22**（刚好达到 Accept 门槛，但存在一个中等严重度问题需修复）

## 发现的问题

| # | 严重程度 | 描述 | 建议修复 |
|---|---------|------|---------|
| 1 | 中 | `readConversations()` (main.js:361) 直接 `JSON.parse(fs.readFileSync(...))` 无 try-catch。若 conversations.json 文件损坏（磁盘故障、手动编辑错误等），将抛出未捕获异常导致主进程崩溃，所有对话数据不可恢复。 | 给 `readConversations()` 包裹 try-catch，损坏时记录错误日志并返回 `[]`（后续 `get-conversations` 会自动创建默认对话）。 |
| 2 | 低 | 对话名称未 trim (renderer.js:242)。用户可输入纯空格名称（如 `"   "`）创建无意义的对话。 | 将 `if (!name) return` 改为 `if (!name || !name.trim()) return`。 |
| 3 | 低 | renderer.js:257-260 的 `if (!currentConvId) { await loadConversations(); }` 是死代码——`loadConversations()` 内部已将 `currentConvId` 回退到第一个可用对话，且 `get-conversations` 在无对话时自动创建默认对话，该条件永远不会为 true。 | 移除死代码块，仅保留 `loadQA()`。 |
| 4 | 低 | 切换对话时输入框（问题/答案）中未提交的内容静默丢失，无任何提示。 | 切换前检查输入框是否有内容，如有则提示用户确认是否丢弃，或将草稿关联到对话保存。 |

## 修订记录

| 轮次 | 初始评分 | 修复后评分 | 主要改进 |
|------|---------|-----------|---------|
| 1 | 18/22 | — | 首次评审 |
| 2 |  |  |  |

## 后续动作

- 缺失的证据：无
- 必须补的修复：问题 #1（readConversations 加 try-catch）
- 建议修复：问题 #2（名称 trim）、问题 #3（移除死代码）、问题 #4（切换时草稿提示）
- 下次复审触发条件：问题 #1 修复后重新评审；若问题 #2-#4 一并修复，可加回 1-2 分。
