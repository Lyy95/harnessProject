# P06 初始质量评分 — 基线快照

> 分支：p06-baseline
> 评估时间：2026-05-21（P06 准备阶段，跑任何 agent 之前）
> 基准 commit：00b8431（kb-015 评估通过，P05 最终状态）
> 评分人：Claude Opus 4.6（静态代码审查 + 功能清单比对）

---

## 评分标准

| 等级 | 分值 | 说明 |
|------|------|------|
| A    | 4    | 完整实现，无明显缺陷，边缘情况覆盖充分 |
| B    | 3    | 核心功能完整，有已知改进空间但不影响主流程 |
| C    | 2    | 功能部分实现，存在明显缺口或已知 bug |
| D    | 1    | 缺失或严重不完整，主流程无法走通 |

---

## 产品领域评分

| 产品领域 | 等级 | 分值 | 根据 |
|----------|------|------|------|
| 文档管理（导入/列表/读取/删除） | B | 3 | 四项操作均实现；缺：文件名冲突无提示、无文件大小上限检查、删除无撤销 |
| 分块与索引（chunk/TF-IDF/搜索） | B | 3 | 分块 + TF-IDF 搜索已实现，启动时自动补建索引；缺：每次搜索重算 IDF（无缓存），索引健康状态无 UI 显示 |
| 问答与引用（带来源引用的回答） | B | 3 | 引用数据结构完整（docName/snippet/chunkId/score），UI 渲染引用卡片；缺：相关性排序仅 TF-IDF，无文档级别去重 |
| 多轮对话（对话 CRUD + 持久化） | B | 3 | kb-015 实现 5 个 IPC handler，CRUD + 切换 + 重启恢复全部可用；缺：无对话重命名、无跨对话搜索 |
| 可观测性（日志/运行时信号） | C | 2 | `services/logger.js` 存在，写结构化 JSON 到 kb.log，有 500KB 轮转；缺：IPC 调用无自动计时包装，无生命周期事件（窗口开/关），无渲染进程错误捕获，无 UI 查看日志 |

**产品领域小计：14 / 20**

---

## 架构层评分

| 架构层 | 等级 | 分值 | 根据 |
|--------|------|------|------|
| 主进程 / IPC 层（main.js） | B | 3 | 22 个 handler，结构清晰，有 logger 调用；缺：业务逻辑与 I/O 混合在 handler 内，无跨 handler 统一错误处理，writeFileSync 非原子写入 |
| 预加载层（preload.js） | A | 4 | 23 个 API 通过 contextBridge 正确暴露，contextIsolation: true、nodeIntegration: false 严格执行，安全边界无违规 |
| 渲染进程（renderer.js） | B | 3 | 功能完整（319 行），有 escapeHtml 防 XSS；缺：无组件拆分，DOM 操作与数据逻辑混合，对话管理与文档管理代码平铺 |
| 数据持久化（JSON 文件层） | B | 3 | 多文件 JSON（conversations.json / imports.json / chunks/*.json / metadata/*.json）格式规范；缺：无原子写（崩溃可能损坏 JSON），无版本号/schema 校验 |
| 日志服务（services/logger.js） | C | 2 | 结构化日志、多级别、轮转已实现；缺：仅手动调用（无自动包装），无 IPC 性能数据，无未捕获异常钩子，无渲染进程错误转发 |

**架构层小计：15 / 20**

---

## 总分

| 类别 | 得分 | 满分 |
|------|------|------|
| 产品领域 | 14 | 20 |
| 架构层 | 15 | 20 |
| **合计** | **29** | **40** |
| **百分比** | **72.5%** | — |
| **综合等级** | **B-** | — |

---

## P06 改进方向（预期提升到 B+ / 35+）

P06 目标是将**可观测性**从 C 提升到 B 或 A，同时带动主进程/IPC 层和日志服务的评分：

| 目标 feature | 影响的维度 | 预期等级变化 |
|---|---|---|
| kb-016 应用生命周期监控 | 可观测性 | C→B |
| kb-017 IPC 调用性能自动包装 | 主进程/IPC + 日志服务 | B→A, C→B |
| kb-018 全局错误捕获（主/渲染双进程） | 可观测性 + 日志服务 | C→B |
| kb-019 运行时指标仪表板 UI | 可观测性 | B→A（如实现）|
| kb-020 消融实验框架 | 整体验证 | 量化改进证明 |

---

## 代码度量快照（基线）

| 指标 | 值 |
|------|-----|
| main.js 行数 | 463 |
| preload.js 行数 | 26 |
| renderer.js 行数 | 319 |
| services/logger.js 行数 | 49 |
| IPC handler 总数 | 22 |
| preload 暴露 API 总数 | 23 |
| 代码总行数（核心文件） | ~857 |
| 已完成 feature 数 | 15（kb-001 ～ kb-015）|
| 已知 logger.info 调用点 | 8（app_start, import_document, log_import, chunk_document, build_index, search_chunks, migrate_qa_to_conversations, conversation CRUD） |
