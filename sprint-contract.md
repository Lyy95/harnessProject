# Sprint Contract — kb-015 多轮对话历史

## Sprint 目标

在问答面板中添加多轮对话线程管理能力：用户可创建命名对话、在对话间切换、每个对话维护独立的问答历史，数据持久化到本地。

## Done 的定义

- [ ] 用户可点击「新建对话」创建命名对话线程
- [ ] 对话列表显示所有对话，当前活跃对话有视觉标识
- [ ] 用户可点击对话名称切换活跃对话
- [ ] 每个对话的问答历史独立存储，切换后正确显示
- [ ] 用户可在当前对话中添加问答对，自动关联到活跃对话
- [ ] 用户可删除对话（含其中所有问答）
- [ ] 重启应用后对话和问答数据完整恢复
- [ ] 如果没有对话，默认创建「默认对话」以兼容旧版问答数据
- [ ] 实施前创建会话 B（评估者），在提交前完成首次评审
- [ ] `bash scripts/check-architecture.sh` 通过 0 违规
- [ ] `./init.sh` 正常运行

## 不在范围内

- 对话内搜索/过滤功能
- 对话导出/导入
- 对话重命名（内联编辑）
- 对话排序/拖拽
- AI 自动生成对话标题
- UI 外观主题变更

## 数据模型

```json
// conversations.json
[
  {
    "id": "uuid",
    "name": "对话名称",
    "createdAt": "ISO-8601",
    "qa": [
      { "q": "问题", "a": "回答", "citations": [...] }
    ]
  }
]
```

## 涉及的 IPC 通道（建议）

- `create-conversation` — 创建新对话
- `get-conversations` — 获取所有对话列表
- `get-conversation` — 获取单个对话详情（含 QA）
- `add-qa-to-conversation` — 向对话添加问答
- `delete-conversation` — 删除对话
- 迁移：首次启动时将旧 `qa.json` 迁移到默认对话

## 验证方式

1. 手动操作：创建对话 → 添加QA → 切换 → 重启验证
2. 检查 `conversations.json` 数据结构
3. 运行 `scripts/check-architecture.sh` 确认边界
4. 评估者用 `evaluator-rubric.md` 独立评分

## 签名

- 规划者（会话 A 前半部分）：_________
- 生成者（会话 A 后半部分）：_________
- 评估者（会话 B）：_________
- 日期：2026-05-20
